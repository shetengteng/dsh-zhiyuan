# 知源（dsh-zhiyuan）MVP 功能测试 Case

> 返回[测试总览](./2026-09-01-03-dsh-知源MVP测试用例.md)。
> 测试数据：[知源 MVP 测试数据](./2026-09-01-04-dsh-知源MVP测试数据.md)。

## 1. 使用说明

本文件只维护用户可执行的功能流程。测试结果中的整体结论、遗留项和安全风险统一回填到[测试总览](./2026-09-01-03-dsh-知源MVP测试用例.md)。

状态约定：`已通过` 表示已有完整证据；`部分通过` 表示只覆盖了部分步骤或环境；`待测` 表示仍需手工/真实环境执行；`预期失败` 表示当前实现已知不满足，修复后再转为待测。

### 1.1 推荐执行顺序

1. 基础质量：T-001、T-002。
2. 插件和工作台：T-003、T-004、T-005。
3. 导入主流程：T-006、T-007、T-008。
4. 搜索和预览：T-009、T-010、T-011、T-015；固定问题见[搜索场景 Case](./2026-09-01-06-dsh-知源MVP搜索场景Case.md)。
5. 扩展和生命周期：T-012、T-013、T-014、T-016。

## 2. Case 清单

| ID | 优先级 | 功能域 | 前置条件 | 操作步骤 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| T-001 | P0 | 自动化测试 | 已安装依赖，位于仓库根目录 | 执行 `npm test` | 82 项测试全部通过，无失败或未处理 rejection | 已通过 |
| T-002 | P0 | 构建 | 已有当前工作区改动 | 在不覆盖现有改动的前提下执行构建 | `lib/index.js`、`lib/client.js` 能成功生成，源代码和已有改动不丢失 | 已通过 |
| T-003 | P0 | 插件配置 | 可使用 DSH CLI | 执行 `dsh plugin --profile web add .`，再执行 `dsh --profile web --dump-config` | 配置中出现 `dsh-zhiyuan`，Cordis row 的 id 为 `zhiyuan` | 部分通过（配置验证通过；`plugin add` 本轮未重跑） |
| T-004 | P0 | 插件加载/卸载 | 已完成 T-003 | 启动 `dsh web`；打开设置；确认出现“知源”；关闭/卸载插件并观察日志 | Client 能加载设置 section、工具视图不报错；Host/Client disposer 执行后无崩溃；数据目录不被卸载删除 | 部分通过（加载通过；卸载待测） |
| T-005 | P0 | 知识库管理 | 知源工作台已打开 | 新建隔离测试库并填写标题、描述、别名；再次创建同标题的库；刷新或重启后再次打开 | 系统生成 UUID；标题重复拒绝；编辑时不展示 id；描述/别名保留，刷新后数据仍在 | 已通过（Playwright） |
| T-006 | P0 | 单文件导入 | 已有隔离测试库；使用 `test/fixtures/zhiyuan-mvp/供应商合同.md` | 在工作台拖入文件，目标类目填 `合同/2024`，开启“目录不存在则创建”，关闭“保留源目录结构”，开始导入 | 创建目标文件；源文件内容和 mtime 不变 | 已通过（Playwright） |
| T-007 | P1 | 文件夹导入 | 已有隔离测试库；使用 `test/fixtures/zhiyuan-mvp/` 文件夹 | 在工作台拖入文件夹，目标类目填 `批量/2025`，开启“保留源目录结构”后导入 | 白名单文本复制成功；`会议/纪要.md`、`冲突/供应商合同.md` 保留相对目录 | 已通过（Playwright） |
| T-008 | P1 | 导入冲突 | 已导入一次供应商合同 | 再导入同名不同内容的 `冲突/供应商合同.md`；检查返回和磁盘 | 同指纹跳过；同名不同内容不覆盖原文件，安全改名 | 部分通过（冲突改名 UI 通过；同指纹跳过单测通过） |
| T-009 | P0 | 固定搜索 | 已有测试库和测试数据 | 按[搜索场景 Case](./2026-09-01-06-dsh-知源MVP搜索场景Case.md)执行 S-001～S-005；检查 path、行号、片段、别名、类目和空结果 | 5 个问题均按数据文档的预期返回；无命中时 `hits=[]` 且不编造内容 | 部分通过（S-001/S-003/S-005 UI 通过；S-002/S-004 单测通过，UI 待测） |
| T-010 | P0 | 命中预览 | 已完成 T-006；工作台可打开试搜 | 在工作台试搜 `违约`，点击一个命中，再关闭预览 | 搜索不经过模型；命中含库内相对路径、起止行号和片段；只读预览读取完整原文并定位/高亮命中行 | 已通过（Playwright） |
| T-011 | P0 | 本地离线 | 已完成 T-006；记录当前网络状态 | 在页面加载完成后执行本地导入/试搜，并检查非本地请求 | 本地操作成功；加载后的本地操作无新增非本地请求 | 部分通过（本地离线行为通过；物理断网待测） |
| T-012 | P1 | 文件选择和校验 | 已打开工作台导入弹框 | 拖入文件和文件夹；检查「选择文件夹」「选择文件」控件、取消、路径校验和开关 | 拖拽/控件/取消/校验行为正常；原生系统选择器在真实桌面环境可用 | 部分通过（Playwright UI 通过；原生选择器待测） |
| T-013 | P1 | CLI 导入/状态 | 已有「工作库」，且先通过 `--to 合同/2024` 导入过一次 | 使用列库结果中的 `<baseId>` 执行 `/kb ingest <新文件> --base <baseId>`；再执行 `/kb ingest <新文件> --base <baseId> --root`；执行 `/kb status` | 无 `--to` 时复用上次类目；`--root` 明确导入库根；status 可显示队列运行/失败状态 | 部分通过（单测通过；手工 CLI 待测） |
| T-014 | P1 | 编辑和删除 | 已有文件和库 | 在工作台编辑标题、描述、别名；打开文件修改并保存；尝试删除文件和知识库 | 编辑后 catalog 更新；保存后原文更新；删除前必须确认；删除的是知识库副本，源文件仍存在 | 已通过（Playwright） |
| T-015 | P0 | 对话命中预览 | 对话中收到 `kb_search` 命中卡 | 点击命中卡，观察弹框内容、滚动位置和编辑能力 | 命中卡打开完整原文；预览只读；命中行滚动并高亮 | 部分通过（工作台命中预览通过；真实对话命中卡待测） |
| T-016 | P1 | 插件生命周期 | 已完成 T-004；插件数据目录有测试库 | 卸载插件，再检查数据目录；重新安装并打开知源 | 卸载不删除 `dsh-zhiyuan` 数据；重装后 catalog、库和文件仍可列出 | 待测 |

## 3. 执行记录模板

执行 `待测` 或 `部分通过` Case 时，在总览或缺陷记录中补充：

| 字段 | 内容 |
| --- | --- |
| Case ID | 例如 `T-011` |
| 环境 | DSH 版本、profile、操作系统、是否联网 |
| 实际步骤 | 实际点击、命令或自然语言原文 |
| 工具调用 | 工具名、baseId、query、aliases、category；不要记录 token/隐私 |
| 实际结果 | 命中路径、行号、状态、错误文案 |
| 判定 | 通过 / 不通过 / 阻塞 |
| 证据 | 截图或脱敏日志路径 |
