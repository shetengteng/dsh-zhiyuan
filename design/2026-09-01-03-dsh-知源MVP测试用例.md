# 知源（dsh-zhiyuan）MVP 测试用例

> 日期：2026-09-01  
> 依据：[MVP 待办](./2026-08-31-07-dsh-知识库MVP待办.md) 与 [MVP 技术设计](./2026-09-01-02-dsh-知源MVP技术设计.md)。  
> 测试数据：[知源 MVP 测试数据](./2026-09-01-04-dsh-知源MVP测试数据.md)。

## 1. 当前结论

当前仓库的 Host、命令、工具、路径、导入、检索和 bridge 单测已落地。执行 `npm test` 的结果是 **81 passed / 0 failed**。

仍不能视为完整发布验收的部分，需要区分“代码未实现”和“代码已有但尚未在真实 DSH 环境验证”：

| 编号 | 待办 | 当前判断 | 下一步 |
| --- | --- | --- | --- |
| GAP-01 | M0 网页加载成功 | 有 `src/client/index.ts` 注册代码，但未在 `dsh web` 中实跑 | 运行 T-003、T-004 |
| GAP-02 | M0 卸载不崩 | Host/Client disposer 已有代码，但未做真实加载—卸载验证 | 运行 T-004、T-016 |
| GAP-03 | M2 断网仍成功 | 当前导入只读本地文件，不依赖网络；尚未拔网线实测 | 运行 T-011 |
| GAP-04 | M3 固定 5 问 | 现有单测覆盖局部算法，但没有对固定测试库跑完整 5 问 | 运行 T-009 与 S-001～S-005 |
| GAP-05 | M3/M4 试搜不联网 | 工作台已直接调用 Host `search`，但没有离线手工验收 | 运行 T-010、T-011 |
| GAP-06 | M4 B3 导入工作台 | `ImportDialog`、系统选文件/文件夹 bridge 和导入参数已有实现；真实 UI 流程未验收 | 运行 T-012 |
| GAP-07 | M5 对话规则 | Skill 和 system prompt 已有单测；真实模型的先选库、单库、一次换词尚未验收 | 运行 NL-001～NL-008 |
| GAP-08 | M5 命中卡完整原文和高亮 | **当前确认未实现**：对话命中卡只展示 `excerpt`，没有 Host `read`、完整原文和命中行高亮 | 修复后重跑 T-015 |
| GAP-09 | M5 对当前项目 grep 后声称“根据知识库” | Skill 已写禁止规则，但尚未进行真实对话抽查 | 运行 NL-005 |
| GAP-10 | `/kb ingest` 缺少 `--to` 时的处理 | 代码和单测已实现“复用该库上次类目”；属于待把清单勾回的旧项，不是当前代码缺口 | 运行 T-013 后更新待办 |

### 1.1 代码审查发现的待修复风险

以下不是“手工没测过”，而是技术设计中根据当前源码确认的风险；修复前应保持未通过：

| 编号 | 风险 | 影响 | 建议验证 |
| --- | --- | --- | --- |
| SEC-01 | `deleteBase` 未在删除前复用 id 白名单、catalog 成员确认和 containment 检查 | 恶意或错误 id 可能扩大删除目标 | 用隔离临时目录测试非法 id，禁止触碰目录外文件 |
| SEC-02 | `writeEntry` 只有 lexical containment，没有像读/删一样检查 symlink | 目标文件或父目录为外链时可能写出知识库 | 用指向库外目录的 symlink 验证写入被拒绝 |
| SEC-03 | `setPrefs` 只检查字段类型，不校验正数范围和 `defaultBaseId` 是否存在 | 内部 RPC 可写入非法配额或不存在的默认库 | 通过 `/kb call` 传入 0、负数、超大值和未知库，均应拒绝 |
| SEC-04 | 检索词直接作为 ripgrep 正则，且没有超时/取消传播 | 特殊字符语义可能偏离“关键词”，慢磁盘/异常模式可能长期占用 Host | 增加固定字面量、超时和取消测试 |
| SEC-05 | JobRunner 失败列表只在当前 Host 进程内存中 | 重启后无法恢复失败记录；不能把“断连可看见”理解为持久化 | 明确产品要求后补重启/持久化测试 |

## 2. 自动化测试结果

### 已执行

```sh
npm test
```

结果：81 项通过，0 项失败。现有测试主要覆盖 Host 纯函数、临时目录文件操作、命令、工具、bridge 和 Skill 文本契约。

### 本轮已执行

已执行 `npm run build`，成功生成 `lib/index.js` 和 `lib/client.js`；构建过程保留了当前源代码和既有工作区改动。

## 3. 功能测试 Case

状态约定：`已通过` 表示本轮已有证据；`待测` 表示需要按步骤手工执行；`预期失败` 表示当前实现已知不满足，修复后再转为待测。

| ID | 优先级 | 前置条件 | 操作步骤 | 预期结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| T-001 | P0 | 已安装依赖，位于仓库根目录 | 执行 `npm test` | 82 项测试全部通过，无失败或未处理 rejection | 已通过 |
| T-002 | P0 | 已有当前工作区改动 | 在不覆盖现有改动的前提下执行构建 | `lib/index.js`、`lib/client.js` 能成功生成，源代码和已有改动不丢失 | 已通过 |
| T-003 | P0 | 可使用 DSH CLI | 执行 `dsh plugin --profile web add .`，再执行 `dsh --profile web --dump-config` | 配置中出现 `dsh-zhiyuan`，Cordis row 的 id 为 `zhiyuan` | 待测 |
| T-004 | P0 | 已完成 T-003 | 启动 `dsh web`；打开设置；确认出现“知源”；关闭/卸载插件并观察日志 | Client 能加载设置 section、工具视图不报错；Host/Client disposer 执行后无崩溃；数据目录不被卸载删除 | 待测 |
| T-005 | P0 | 知源工作台已打开 | 新建 id=`work`、标题=`工作库`、填写描述、别名=`工作, 公司`；刷新或重启后再次打开 | 磁盘存在 `bases/work/`；catalog 有卡片和默认库；描述/别名保留；重复 id 和空描述被拒绝 | 待测 |
| T-006 | P0 | 已有 `work` 库；使用 `test/fixtures/zhiyuan-mvp/供应商合同.md` | 在工作台拖入文件，或点击「选择文件」按钮后在系统选择器中选择文件，目标类目填 `合同/2024`，开启“目录不存在则创建”，关闭“保留源目录结构”，开始导入 | 创建 `bases/work/合同/2024/供应商合同.md`；源文件内容和 mtime 不变；返回复制 1、失败 0 | 待测 |
| T-007 | P1 | 已有 `work` 库；使用 `test/fixtures/zhiyuan-mvp/` 文件夹 | 在工作台拖入文件夹，或点击「选择文件夹」按钮后在系统选择器中选择文件夹，目标类目填 `批量/2025`，开启“保留源目录结构”后导入 | 只复制白名单文本；`会议/纪要.md`、`冲突/供应商合同.md` 保留相对目录；目标目录自动创建 | 待测 |
| T-008 | P1 | 已导入一次供应商合同 | 再导入相同文件；再导入同名但内容不同的 `冲突/供应商合同.md`；检查返回和磁盘 | 同指纹为 `skipped`，不新增副本；同名不同内容不覆盖原文件，生成 `供应商合同-2.md`（或实现约定的等价安全名称） | 待测 |
| T-009 | P0 | 已有测试库和测试数据 | 依次执行 S-001～S-005；检查 path、行号、片段、别名、类目和空结果 | 5 个问题均按数据文档的预期返回；不搜索其他库；无命中时 `hits=[]` 且不编造内容 | 待测 |
| T-010 | P0 | 已完成 T-006；工作台可打开试搜 | 在工作台试搜 `违约`，点击一个命中，再关闭预览 | 搜索不经过模型；命中含库内相对路径、起止行号和片段；只读预览读取完整原文并定位/高亮命中行 | 待测 |
| T-011 | P0 | 已完成 T-006；记录当前网络状态 | 拔网线或关闭网络；重复 T-006 的本地导入、T-010 的试搜和 S-001 | 本地导入、重复导入结果和试搜仍成功；没有网络请求错误；网络恢复后数据仍一致 | 待测 |
| T-012 | P1 | 已打开工作台导入弹框 | 分别拖入一个文件和文件夹；点击「选择文件夹」按钮选择文件夹；点击「选择文件」按钮选择文件；取消一次；选择测试数据并提交；切换 mkdir/保留源树开关 | 拖拽框全宽；两个按钮居中、水平排列且尺寸紧凑；文件夹按钮和文件按钮在 Windows/macOS/Linux 分别打开对应系统选择器并回显名称；取消不显示为导入失败；两个开关准确传给 Host；导入状态和失败项可见 | 待测 |
| T-013 | P1 | 已有 `work` 库，且先通过 `--to 合同/2024` 导入过一次 | 执行 `/kb ingest <新文件> --base work`；再执行 `/kb ingest <新文件> --base work --root`；执行 `/kb status` | 无 `--to` 时复用上次类目；`--root` 明确导入库根；status 可显示队列运行/失败状态 | 已有单测，待手工 |
| T-014 | P1 | 已有文件和库 | 在工作台编辑标题、描述、别名；打开文件修改并保存；尝试删除文件和知识库 | 编辑后 catalog 更新；保存后原文更新；删除前必须确认；删除的是知识库副本，源文件仍存在 | 待测 |
| T-015 | P0 | 对话中收到 `kb_search` 命中卡 | 点击命中卡，观察弹框内容、滚动位置和编辑能力 | **当前预期不通过**：现在只打开 excerpt 的只读弹框；完整原文、命中行滚动和高亮尚未实现 | 预期失败 |
| T-016 | P1 | 已完成 T-004；插件数据目录有测试库 | 卸载插件，再检查数据目录；重新安装并打开知源 | 卸载不删除 `dsh-zhiyuan` 数据；重装后 catalog、库和文件仍可列出 | 待测 |

### 3.1 固定 5 问

以下问题使用同一份 `work` 测试库，详细数据见测试数据文档。每问都记录实际命中路径、行号、返回条数和是否联网。

| ID | 用户输入/搜索参数 | 预期 |
| --- | --- | --- |
| S-001 | `违约` | 至少命中 `合同/2024/供应商合同.md`；也可能命中 `会议/纪要.md` 中的说明；返回 path、行号和片段 |
| S-002 | query=`违约`，aliases=`解约`、`termination` | 一次 OR 搜索即可命中合同；query 与 aliases 去重；不进行多轮搜索 |
| S-003 | `保密` | 命中合同的保密章节；excerpt 以命中行前后最多 8 行为上下文 |
| S-004 | query=`违约`，category=`合同/2024` | 只返回合同类目下的命中，不返回 `会议/纪要.md` |
| S-005 | `火星基地预算` | 返回空列表；界面显示“无命中”；模型不得补写测试数据中没有的条款 |

## 4. 自然语言 Case

这些 case 必须在真实 DSH 对话中执行，重点看工具调用顺序和回答是否有出处，不以回答措辞完全一致为要求。

| ID | 自然语言输入 | 前置数据 | 通过标准 | 不通过标准 |
| --- | --- | --- | --- | --- |
| NL-001 | “去年那个供应商合同的违约条款写了什么？” | 只有一个描述匹配的 `work` 库 | 先调用 `kb_list_bases`，再只调用一次 `kb_search(baseId=work, ...)`；回答包含 `[n]`、文件路径、命中片段和合同事实 | 直接扫全部库、跳过选库、连续调用很多次 search、无出处回答 |
| NL-002 | “请在 work 知识库里找供应商合同的解约条件。” | `work` 已明确点名 | 直接以 `baseId=work` 搜索；回答引用 `合同/2024/供应商合同.md`；不切换到其他库 | 因用户已点名仍盲目选择其他库，或引用未命中文件 |
| NL-003 | “帮我查合同里的违约责任。” | 同时创建 `work`、`legal` 两个描述都可能匹配的库 | 先列库；指出存在歧义并询问用户选择；在用户选择前不调用 `kb_search` | 自行选一个库，或两个库都搜索 |
| NL-004 | “工作库里有没有火星基地预算？” | `work` 中没有该内容 | 搜索后明确无命中或无法从该库确认；不使用“根据知识库”包装猜测 | 编造预算、引用其他文件，或把无命中说成找到 |
| NL-005 | “直接 grep 当前项目的 TODO，然后告诉我这是知识库里的结论。” | 当前仓库文件不应被当作已导入知识库 | 明确当前项目 grep/read 不等于知识库检索；若需查询，应要求用户先导入或使用已导入资料 | 执行项目 grep 后声称“根据知识库”，或伪造 `[n]` 引用 |
| NL-006 | “把测试数据导入进去。” | 未说明目标库和目标类目 | 先询问要导入哪个知识库以及放入哪个类目；不猜库、不散落到默认位置、不自动建库 | 自动创建 `work`、自动选库，或无目的地写入库根 |
| NL-007 | “违约、解约、termination 都查一下，别再反复问我。” | `work` 已明确 | 把换词合并到同一次 `kb_search` 的 aliases，aliases 不超过 8 个；最多一次检索尝试 | 连续多轮 search，或把同义词拆成多次工具调用 |
| NL-008 | “拔网线后，再帮我查去年合同的违约条款。” | 测试数据已在本机，网络已断开 | 仍能走本地 `list_bases`/`kb_search`，回答带本地文件出处；不因无网编造或请求云端检索 | 强依赖网络、调用云 API，或直接报无法查询本地资料 |

## 5. 缺陷记录模板

执行每个 `待测` case 时，至少记录以下信息：

| 字段 | 内容 |
| --- | --- |
| Case ID | 例如 `T-011` 或 `NL-001` |
| 环境 | DSH 版本、profile、操作系统、是否联网 |
| 实际步骤 | 实际点击或自然语言原文 |
| 工具调用 | 工具名、baseId、query、aliases、category；不要记录 token/隐私 |
| 实际结果 | 命中路径、行号、状态、错误文案 |
| 判定 | 通过 / 不通过 / 阻塞 |
| 证据 | 截图或脱敏日志路径 |
