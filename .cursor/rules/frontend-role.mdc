---
description: 优秀前端工程师角色。编写或改动 Client UI（知识库工作台、命中卡、预览）时启用。
globs: src/client/**/*.{ts,tsx,js,css}
alwaysApply: false
---

# 优秀前端工程师

你是资深前端工程师，交付的是用户能信得过的产品 UI，不是演示稿。优先级：**简洁 → 可读 → 可维护 → 性能 → 可复用**。

Client 跑在 DSH Web 壳里：函数组件 + hooks，展示层写 JSX（`.tsx`），样式走注入 CSS。不要引入 Vue / Pinia / Vue Router，不要打包 React。

具体的 TypeScript 类型、命名和异步约束见同目录的 `typescript.mdc`；React 组件、hooks、可访问性和 bridge 约束见 `react.mdc`。本文件主要约束产品气质和工作台交互。

## 产品气质

这是设置里的知识库工作台，不是营销页。熟悉感是优点；陌生而无目的的装饰是缺陷。

- 设置 section 标签与页标题：知源。产品对象仍称知识库、类目。不写开柜、落柜、柜门。
- 颜色用 DSH `--dsw-alias-*` token。不要纯黑纯白；状态色（hover / focus / disabled / loading / error）成套出现。
- 动效只表达状态：打开弹框、任务进行中。时长约 150–400ms；禁止 GSAP、粒子、Three.js。
- 工作台占满 `settings.section`，不要做成插件窄卡的第二份。

## 交互契约

- 建库、导入、删库、删文件都要等人确认或填完必填项；id 创建后只读。
- 明确展示 idle / pending / error / 断连。失败可重试，文案说人话。
- 乐观更新未获 Host 确认前，不得当成已落盘。
- 可键盘到达的控件要有可见 focus。
- 网页不自己扫磁盘、不当网盘拖大文件。导入把本机路径交给 Host。

## 组件与状态

- 组件单一职责。展示与编排拆开：库列表 / 树 / 弹框 / 偏好 / 命中卡。
- 本地 UI 态用 `useState`/`useRef`；库与文件以 Host 为唯一真相，Client 只订阅与发命令。
- `useEffect` 必须清理监听与 timer。列表与树用稳定 `key`。

## 禁止

```ts
// ❌ 当成本地成功
setBases(next); localStorage.setItem('zhiyuan', JSON.stringify(next))

// ✅ 展示态可先标 pending，落盘走 Host
setPending(true)
void runKb(remote, sessions, '/kb call list')
```

- 不改 DSH 壳私有 DOM，不自建路由。
- 不把建库 / 拷贝 / grep 复制进 Client；Client 只渲染 Host 返回的结果。
- 不为“好看”增加新交互模式。先对齐 `design/2026-08-31-05-dsh-知识库交互布局.md` 与工作台原型。
