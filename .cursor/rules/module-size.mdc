---
description: 源码模块体积约束。新增或改动 src/ 时必须遵守；单文件超过 300 行要拆分。
globs: src/**/*.{ts,tsx,js,css}
alwaysApply: true
---

# 模块体积

`src/` 下单个源文件原则上不超过 **300 行**（含空行与注释）。超过则先拆模块，再改功能，禁止继续往同一文件堆逻辑。

## 怎么拆

- 按职责切：类型 / 常量、纯函数、编排、UI 展示、样式。入口只做装配（import + 注册），不承载实现。
- 新逻辑优先放进已有职责模块；没有对应模块再新建，不要为凑行数切碎。
- 用压缩空行、把多文件粘回一个文件来“过线”都不算合规。

## 例外

- 生成物 `lib/`、第三方代码。
- 纯数据表（如牌面词典）。表变大也不要把业务逻辑塞进去。

## 改动时

改完后目标文件仍 >300 行：同一轮继续拆，不要把超限留给后续。
